{
  "name": "campusflow-root",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "build": "cd frontend && npm install && npm run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "next": "16.2.9"
  }
}
