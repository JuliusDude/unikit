# Project Context

## Current Objective
- [ ] Implement user requests according to the rules defined in `Rules.md`.

## Active Tasks
- [ ] Apply the new Universal Design System (extracted from `/tasks`) to all remaining dashboard pages: Notices, Attendance, Flashcards, MCQ Quiz, Smart Tools, and Whiteboard.
## Active Tasks
## Active Tasks
- [x] Converted Developer Mode auto-login into a "Developer Login" button on the `/login` page.
- [x] Refactored DashboardPage to fetch `tasks` and `attendance` globally and pass them as props, eliminating network waterfall from 7 widgets.
- [x] Install impeccable skill using `npx skills add`.
- [x] Restart servers to apply Developer Mode changes.

## Completed Tasks
- [x] Run the backend program via `npm start`. (Running on Port 4000)
- [x] Install frontend dependencies and start the frontend Next.js server (`npm run dev`). (Running on Port 3001)
- [x] Initialized `Rules.md` with core workflow and guidelines.
- [x] Initialized `CONTEXT.md` to track project state.

## Important Notes & Constraints
- (Add any specific architectural decisions, constraints, or context details here)
